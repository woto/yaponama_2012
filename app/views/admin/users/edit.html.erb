<% @meta_title = "Редактирование профиля" %>

<%= page do |p| %>

  <%= p.breadcrumb do |b| %>
    <%= b.item 'Главная', root_path %>
    <%= b.item 'Администрирование', [:admin, :root] %>
    <%= b.item 'Пользователи', [:admin, @resource_class] %>
    <%= b.item @resource.to_label, [:admin, @resource] %>
    <%= b.item @meta_title %>
  <% end %>

  <%= p.title do %>
    <%= page_header %>
  <% end %>

  <%= p.content :xs do %>
    <%= render 'form'%>
  <% end %>

<% end %>
