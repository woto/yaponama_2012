<% if @item.status == 'cancel' %>
  <li><strong> <%= link_to 'Перезаказать', url_for(:action => :new, :product_id => params[:id], :return_path => params[:return_path], :skip => true ) %> </strong></li>
<% end %>
