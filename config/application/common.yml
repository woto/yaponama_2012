defaults: &defaults
  share_buttons: true
  send_pings: true
  suppress_exceptions: true

development:
  <<: *defaults
  share_buttons: false
  send_pings: false
  suppress_exceptions: true

test:
  <<: *defaults
  share_buttons: false

production:
  avtorif.ru:
    <<: *defaults
  zapstok.ru:
    <<: *defaults
