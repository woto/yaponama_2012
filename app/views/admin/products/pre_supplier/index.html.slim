= somebody_tabs do
  ul.breadcrumb : li Смена статуса на "#{Rails.configuration.products_status['pre_supplier']['title']}"

  = form_tag smart_route({:postfix => [:products, :pre_supplier_index]}, jaba), :class => "dirtyforms" do |f|

    = render :partial => 'shared/grid'

    .form-actions
      =' link_to "Назад", params[:return_path], :class => "btn"
      = submit_tag "Ок", :class => "btn btn-primary"
