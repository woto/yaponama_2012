$('#modal_form').modal('hide')

<% id = "#{@resource_class.to_s.downcase}_#{@resource.id}" %>

$('#<%= id %>').replaceWith("<%= j render 'grid_item', item: @resource %>")
// TODO вернуть подсветку измененного элемента
//$('#<%= id %>').effect('highlight')

<%# Это копия из grid_filter.js.erb Если меняется количество товаров, то должен измениться хинт с кол-вом выделенных товаров, к другим видимо это не относится %>

<% f = nil %>
<% twbs_form_for @grid, url: '' do |form| %>
  <% f = form.dup %>
<% end %>

$('#grid-toolbar-selected').replaceWith('<%= j render 'grid_toolbar_selected', f: f %>');

$('#grid-toolbar-actions').replaceWith('<%= j render 'grid_toolbar_actions', f: f %>');
