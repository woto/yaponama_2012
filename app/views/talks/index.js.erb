(function() {
  var App;

  App = typeof exports !== "undefined" && exports !== null ? exports : this;

  <% @talk = prepare_talk_form %>
  $('#new_talk').replaceWith("<%= j render 'form' %>");
  $('#talk-log').replaceWith("<%= j render 'log' %>");
  $('#important').html("<%= j render 'important' %>");
  App.nanoscroller_init()
  App.autosize_init()

}).call(this);
