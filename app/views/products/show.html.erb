<%= somebody_tabs do %>

  <% title_show %>
  <% @meta_title_small =  @resource.to_label %>

  <%= breadcrumb true do |b| %>
    <%= b.item @meta_title + " " + @meta_title_small %></a></li>
  <% end %>

  <%= title_show %>

  <%= render 'smart_show' %>
<% end %>
