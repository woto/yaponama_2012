<% if Rails.configuration.payment_systems[@resource.payment_type][:system] == 'sberbank' %>

  <%= alert 'info' do %>
    Тут текст, объясняющий покупателю, что делать с квитанцией и т.д. (+ взять этот текст со страницы самой квитанции)
  <% end %>

  <%= link_to 'Распечатать квитанция Сбербанка', print_user_payment_path, class:"btn btn-primary", target:"_blank" %>

<% elsif Rails.configuration.payment_systems[@resource.payment_type][:system] == 'robokassa' %>

  <%= alert 'info' do %>
    Тут текст, объясняющий покупателю, что платежи принимаются через Робокассу, что будет произведена переадресация, а так же включить логотип Робокассы
  <% end %>

  <% payment_service_for @resource.id, CONFIG.active_merchant['user'], :amount => @resource.amount, :secret => CONFIG.active_merchant['robokassa_pass_1'], :service => :robokassa, :currency => @resource.payment_type do |service| %>

      <% service.description truncate(@resource.payment_destination, :length => 100, :omission => '...') %>
      <% service.email @resource.try(:profile).try(:emails).try(:first).try(:to_label) %>

      <%= submit_tag 'Оплатить с помощью Робокассы', :class => "btn btn-primary" %>
  <% end %>

<% end %>
