#$(document).on 'page:update', ->
#
#  $("nav").headroom
#
#    offset: $("nav").height(),
#
#    # callback when pinned, `this` is headroom object
#    onPin: ->
#      #$(this.elem).addClass('aaa')
#
#    
#    # callback when unpinned, `this` is headroom object
#    onUnpin: ->
#      #$(this.elem).addClass('bbb')
