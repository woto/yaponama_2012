<script>
  $(function(){
    
    $("#zzz").on("click", function(){
      var data = CKEDITOR.instances.editable.getData();
      alert(data);
    });
  })
    // Your code to save "data", usually though Ajax.
</script>

<%- model_class = Admin::SpareInfo -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:catalog_number) %>:</strong></dt>
  <dd><%= @admin_spare_info.catalog_number %></dd>
  <dt><strong><%= model_class.human_attribute_name(:manufacturer) %>:</strong></dt>
  <dd><%= @admin_spare_info.manufacturer %></dd>
  <dt><strong><%= model_class.human_attribute_name(:content) %>:</strong></dt>
  <dd>
    <div id="editable" contenteditable="true">
      <%= raw Sanitize.clean @admin_spare_info.content, Rails.configuration.sanitize_config %>
    </div>
  </dd>
</dl>

<button id="zzz"></button>




<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              admin_spare_infos_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_admin_spare_info_path(@admin_spare_info), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              admin_spare_info_path(@admin_spare_info),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
