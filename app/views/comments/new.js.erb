$('#new_comment').remove()
$("#comment-<%= params[:parent_id] %>").append("<%= escape_javascript(render partial: "comments/form") %>");  
$(function(){
  window.initSimpleCkeditor();
  window.initDirtyForms();
  window.initCkeditorHelper();
})
