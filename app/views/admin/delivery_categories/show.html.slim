- model_class = DeliveryCategory
.page-header
  h1 =t '.title', :default => model_class.model_name.human
 
dl.dl-horizontal
  - @delivery_category.class.column_names.each do |column_name|
      dt
        strong 
          = model_class.human_attribute_name(column_name) 
          | :
      dd.clearfix = eval "@delivery_category.#{column_name}"

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), admin_delivery_categories_path, :class => 'btn'
  '
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_admin_delivery_category_path(@delivery_category), :class => 'btn'
  '
  = link_to t('.destroy', :default => t("helpers.links.destroy")), admin_delivery_category_path(@delivery_category), :method => 'delete', :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
