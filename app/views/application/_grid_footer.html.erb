<div id="grid-footer">
  <% if @items.num_pages > 1 %>
    <%= p.footer class: 'clearfix' do %>
      <%= paginate @items, :theme => :toolbar, :window => 2, :remote => true %>
    <% end %>
  <% end %>
</div>
