<%= simple_form_for [:admin, @name], :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.association :user %>
  <%= f.input :name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                admin_names_path, :class => 'btn' %>
  </div>
<% end %>
