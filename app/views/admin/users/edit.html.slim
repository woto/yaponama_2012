- model_class = User

.page-header
  = render :partial => "shared/client_button", :locals => {:user_id => @user.id, :user_label => @user.new_record? ? "Новый покупатель" :  @user.to_label}

= render :partial => 'form'
