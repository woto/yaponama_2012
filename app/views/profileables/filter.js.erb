$('#items').replaceWith('<%= j render :partial => 'shared/grid' %>');

$('#items').effect("highlight");

$('#toolbar-selected').replaceWith('<%= j render :partial => 'shared/grid_toolbar_selected' %>');

$('#toolbar-per-page').replaceWith('<%= j render :partial => 'shared/grid_per_page' %>')

window.bootstrapperize()
