<%= f.form_group method.to_s.chomp('_id') do %>

  <%= f.control_label(extra[:label] || method, asterisk: extra[:asterisk]) %>

  <%= a2 do %>
    <%= f.select method, choices, options, html_options %>
    <%= f.error method %>
    <%= f.hint method %>
  <% end %>

<% end %>
