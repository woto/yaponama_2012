User-Agent: *
Allow: /
Sitemap: http://www.<%= "#{Rails.configuration.site['host']}" %>/sitemap.xml.gz
Host: www.<%= "#{Rails.configuration.site['host']}" %>

<% @blocked_bots.each do |bot| %>
User-agent: <%= bot.user_agent %>
Disallow: /

<% end %>
